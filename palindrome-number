class Solution {
public:
    bool isPalindrome(int x) {
        long n=x,d,nn=0;
        while(n>0){
            d=n%10;
            nn=nn*10+d;
            n=n/10;
            if(nn>INT_MAX)
                break;
        }
        if(nn == x)
            return 1;
        return 0;
    }
};
